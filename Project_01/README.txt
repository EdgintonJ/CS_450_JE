This was a group project where we had to take a given "Sudoku" puzzle and check if 
the given set was correct (no repeating numbers in row, column, and subgrid).

We split the project into 3 parts with one of us checking rows, the other checking columns, 
and myself checking sub-grids.
